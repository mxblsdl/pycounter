[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pycounter"
version = "0.2.0"
description = ""
readme = "README.md"
authors = [
    { name = "mxblsdl", email = "maxblasdel@gmail.com" }
]
dependencies = [
    "rich>=13.9.2",
    "setuptools>=75.5.0",
]

[project.scripts]
count = "pycounter.cli:main"

# Optional uv-specific configuration
